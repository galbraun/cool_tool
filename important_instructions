In order to fix auth problem with SQLite in a very effective way:

1. list all the secrets from AWS secretmanager in neo-dev profile.
2. use the first one in `curl https://<secret_name>.neo.security`
